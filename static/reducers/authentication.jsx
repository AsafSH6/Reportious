// import {GET_REPORTS, reports} from "../constants";

const defaultState = {
    isLoggedIn: true
};

export default (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }}